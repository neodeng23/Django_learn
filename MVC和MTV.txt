传统的MVC
    M model 模型层，主要用于对数据库层进行封装
    V view 视图层，用于向用户展示结果（如何显示和显示的内容）
    C controller 控制，用于处理请求，获取数据，返回结果（重要）

MTV模式
    M model 模型层，负责与数据库交互
    T Template 模板层，负责呈现内容到浏览器（如何显示）
    V view 视图层，核心，负责接收请求，获取数据，返回结果（显示的内容）
